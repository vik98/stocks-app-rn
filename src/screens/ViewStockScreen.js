import React from "react";
import { View, Text, StyleSheet } from "react-native";

const ViewStockScreen = () => {
  return (
    <View>
      <Text>View Stock Screen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default ViewStockScreen;
