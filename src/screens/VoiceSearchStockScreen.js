import React from "react";
import { View, Text, StyleSheet } from "react-native";

const VoiceSearchStockScreen = () => {
  return (
    <View>
      <Text>Voice Search Stock Screen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default VoiceSearchStockScreen;
