import React from "react";
import { View, Text, StyleSheet } from "react-native";

const SearchStockScreen = () => {
  return (
    <View>
      <Text>Search Stock Screen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default SearchStockScreen;
